{
  "buildCommand": "yarn build && yarn storybook:build",
  "outputDirectory": "packages/storybook/storybook-static",
  "installCommand": "yarn install",
  "build": {
    "env": {
      "STORYBOOK_BRANCH_NAME": "$VERCEL_GIT_COMMIT_REF"
    }
  }
}